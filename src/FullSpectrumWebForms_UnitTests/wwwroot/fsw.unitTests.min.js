/// <reference path="../../../../../fullSpectrumWebForms/wwwroot/js/core/controlBase.ts" />
/// <reference path="../../../../../fullSpectrumWebForms/wwwroot/js/controls/html/htmlControlBase.ts" />
/// <reference path="../../../../node_modules/@types/jquery/index.d.ts" />
var controls;
(function (controls) {
    class UTManager extends core.controlBase {
        initialize(type, index, id, properties) {
            super.initialize(type, index, id, properties);
        }
        getElementTextFromId(id) {
            let elem = $('#' + id);
            if (elem.length == 0)
                return null;
            return elem.text();
        }
        getFSWControlText(id) {
            let control = core.manager.getControl(id);
            if (control && control.element) {
                return control.element.val();
            }
            else
                return null;
        }
        getElementValFromId(id) {
            let elem = $('#' + id);
            if (elem.length == 0)
                return null;
            return elem.text();
        }
        getFSWControlVal(id) {
            let control = core.manager.getControl(id);
            if (control && control.element) {
                return control.element.val();
            }
            else
                return null;
        }
    }
    controls.UTManager = UTManager;
})(controls || (controls = {}));
core.controlTypes['UTManager'] = () => new controls.UTManager();
//# sourceMappingURL=UTManager.js.map